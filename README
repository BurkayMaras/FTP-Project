FTP Client Project
This is a Python project that provides a user interface for interacting with an FTP server. The FTP client supports uploading and downloading files, renaming files, and listing directory contents on a remote server.

Features
Upload Files: Upload files from your local machine to a remote FTP server.
Download Files: Download files from a remote FTP server to your local machine.
Rename Files: Rename files on the remote FTP server.
List Files: List the contents of directories on the remote FTP server.
Prerequisites
Python 3.x
ftplib module (included in Python's standard library)
